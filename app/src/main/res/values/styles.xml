<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MarginAndPadding">
        <item name="android:layout_margin">2dp</item>
        <item name="android:padding">1dp</item>
    </style>

    <style name="EventName" parent="MarginAndPadding">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:textSize">@dimen/bootstrap_h3_text_size</item>
    </style>

    <style name="HorizontalLabelValuePair" parent="MarginAndPadding">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="HorizontalLabel" parent="MarginAndPadding">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/bootstrap_h4_text_size</item>
        <item name="android:layout_weight">0</item>
    </style>

    <style name="HorizontalValue" parent="MarginAndPadding">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textSize">@dimen/bootstrap_h4_text_size</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">end</item>
        <item name="android:textAlignment" tools:targetApi="jelly_bean_mr1">textEnd</item>
    </style>
</resources>
